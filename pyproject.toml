[project]
name = "warehouse-inventory-env"
version = "0.1.0"
description = "Warehouse Inventory OpenEnv Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
